import { loadInputLineArray } from "src/helpers";

export function part1(): string {
  const instructions = loadInputLineArray(2);

  return "";
}

export function part2(): string {
  const instructions = loadInputLineArray(2);

  return "";
}
