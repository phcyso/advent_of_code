import { describe, it } from "vitest";

describe("day8", () => {
  describe("part1", () => {
    it.todo("parses the sample input");
    it.todo("solves the sample input");
    it.todo("solves the full input");
  });

  describe("part2", () => {
    it.todo("solves the sample input");
    it.todo("solves the full input");
  });
});
